<Window x:Class="CompteurCarnet.ListeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Liste des carnets" Height="600" Width="500"
        Background="{StaticResource BgBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Liste des carnets"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <Button x:Name="BtnImporter" Grid.Row="1" Content="Importer des fichiers JSON"
                Style="{StaticResource MenuButton}"
                Background="{StaticResource GreenBrush}"
                Click="BtnImporter_Click"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CarnetsList"/>
        </ScrollViewer>
    </Grid>
</Window>
